import React from "react";

const Search = () => {
    const searchStyle = {
        fontStyle: '20px'
    };
    return <input style={searchStyle}
                  placeholder="search"/>
}

export default Search;